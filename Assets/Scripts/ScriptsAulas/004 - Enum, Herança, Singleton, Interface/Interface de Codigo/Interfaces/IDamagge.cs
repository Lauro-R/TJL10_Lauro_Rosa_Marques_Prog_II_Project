using UnityEngine;

public interface IDamagge //I**** antes do nome da interface para ficar facil de identificar a mesma no código
{
    public void SofrerDano(float dano);
}//a interface é uma forma de organizar o código e garantir que comportamentos ocorram
