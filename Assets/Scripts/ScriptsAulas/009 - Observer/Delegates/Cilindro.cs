using UnityEngine;

public class Cilindro : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //ObserverManager.Instance.Morrer = null;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
